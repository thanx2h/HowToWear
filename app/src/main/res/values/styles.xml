<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar"></style>

    <!-- App theme for Toolbar. -->
    <style name="AppThemeForToolbar" parent="Theme.AppCompat.Light.NoActionBar"></style>



</resources>
